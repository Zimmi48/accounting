# Project Context

## Core Context (Archived Sessions)
- ## Core Context
- ## Recent Approvals & Fixes

## Learnings
- 2026-05-17T11:49:00Z: Minimal #53 follow-up keeps theme persistence frontend-only in `src/Frontend.elm` by hydrating `theme` from `?theme=` during `init`/`UrlChanged`, preserving that query on internal navigation, and leaving `Import`/`Json`/`NotFound` page-local state out of the URL. Keep `tests/FrontendTests.elm` focused on the actual requirement: bootstrap from query plus route-preserving query rewrites, not round-trips for unrelated page state. Validation: `elm-format src/ tests/ --yes`, both `lamdera make` targets, `npm test`, `curl` confirmed HTTP 200 on the already-running port-8000 Lamdera server in this repo, and a fresh `lamdera live --port=8010` instance also served HTTP 200.
- 2026-05-15T15:40:58Z: Issue #52 revision moved transaction-list summary headers above their corresponding periods. Backend pagination in `src/Backend.elm` now emits month summaries before month rows and hoists any available year summary to the first loaded month of that year within the page; frontend merge logic in `src/Frontend.elm` normalizes appended pages so a later year-boundary response still renders the year header above already-loaded newer months. Regression coverage lives in `tests/BackendTests.elm` (boundary markers) and `tests/FrontendTests.elm` (replace/append ordering). Validation: `elm-format src/ tests/ --yes`, `./check-codecs.sh`, both `lamdera make` targets, `npm test` (57/57), and `lamdera live` with HTTP 200 on port 8010 because port 8000 was occupied in the shared environment.

- 2026-05-15T11:26:00Z: Issue #32 needed the transaction toggle marker to own its visible state in `src/Frontend.elm`, not borrow it from parent text color. The durable frontend pattern is `transactionCheckVisualState` → `transactionCheckIndicator`/`transactionCheckButtonStyle`, with `tests/FrontendTests.elm` covering explicit checked-vs-unchecked marker state, targeted optimistic toggles, and the backend refresh seam that must preserve untouched rows.

- 2026-05-15: `src/Frontend.elm` already had `viewAmount` as the canonical cents-to-decimal formatter for UI text. Reusing it in spending-editor hydration (`transactionLineFromSpendingTransaction`, `SpendingDetails`) is safer than keeping a second helper, and `tests/FrontendTests.elm` now guards the `viewAmount` ↔ `parseAmountValue` round-trip seam. User preference on this fix: keep #40 isolated from unrelated issue work and avoid Evergreen migration work unless explicitly requested.

- 2026-05-15T07:48:48Z: Completed issue #40 by consolidating frontend amount formatting. Removed duplicate `formatAmountValue` helper and reused canonical `viewAmount` for all cents-to-decimal conversions in spending editor hydration (`transactionLineFromSpendingTransaction`, `SpendingDetails`). Added round-trip test coverage for `viewAmount` ↔ `parseAmountValue` in `tests/FrontendTests.elm`. Updated skill doc `.squad/skills/frontend-amount-formatting/SKILL.md`. Validation: elm-format, lamdera make src/Frontend.elm, lamdera make src/Backend.elm, npm test (33/33 pass), HTTP 200. Decision merged to `.squad/decisions/decisions.md`. Commit: 755a76a (Fix #40 by reusing viewAmount).
- 2026-05-15T07:52:00Z: Issue #47 restored frontend spending-submit gating in `src/Frontend.elm`. `canSubmitSpending` now requires the full signed invariant `sum credits == sum debits == total` in addition to non-zero total and complete rows, and the `Submit` branch reuses that same guard so disabled-button semantics and dispatched submissions cannot drift. Regression coverage lives in `tests/FrontendTests.elm` for both positive and negative mismatch cases. Validation: elm-format, `./check-codecs.sh`, both `lamdera make` targets, `npm test` (41/41), and `lamdera live` with HTTP 200 on port 8010 after 8000 was already occupied in the shared environment.
- 2026-05-15T15:40:58Z: Issue #52 revision committed (e36ea18) — transaction-list summary rows now appear as headers leading their periods. Backend pagination in `src/Backend.elm` emits month summaries before month rows and hoists year summary to first loaded month of that year within each page; frontend merge logic in `src/Frontend.elm` normalizes appended pages so a late year-boundary response still renders year header above already-loaded newer months of that year. Regression coverage: `tests/BackendTests.elm` (month summary → month rows) and `tests/FrontendTests.elm` (year summary → first month of year on replace + load-more paths). Boundary markers like `Y 2025 → M 2025-04 → T 2025-04` assert top-of-period invariant explicitly. Validation: elm-format, lamdera make src/Frontend.elm, lamdera make src/Backend.elm, npm test (57/57), HTTP 200 on port 8010. Vasquez approved for production.

- 2026-05-16T00:00:00Z: Month folding for the paginated transaction list stays frontend-local in `src/Frontend.elm` by grouping normalized `GroupTransactionListItem`s into standalone year headers plus foldable month sections rendered with native `<details open>`. That keeps months expanded by default, preserves existing pagination and refresh-depth behavior, and avoids introducing shared Lamdera model fields or Evergreen migration work. Regression coverage lives in `tests/FrontendTests.elm` through `groupTransactionViewSections` and `groupTransactionMonthSectionItems`, which prove year headers remain standalone and folded months retain only the month summary row. Validation: `elm-format src/ tests/ --yes`, `./check-codecs.sh`, both `lamdera make` targets, `npm test` (66/66), and `lamdera live --port=8010` with HTTP 200 because port 8000 was occupied in the shared environment.

- 2026-05-16T11:26:24Z: Revised the folded-month pagination seam in `src/Frontend.elm` by routing both scroll-triggered and fold-triggered older-history loads through pure planning helpers (`toggleGroupTransactionMonthFoldPlan`, `groupTransactionsViewportLoadMorePlan`, `groupTransactionsLoadMorePlan`) before turning them into commands. Keep regression coverage in `tests/FrontendTests.elm` focused on the real fold -> viewport recheck -> `RequestGroupTransactions` path so pagination fixes prove the async seam instead of only viewport math. Validation: elm-format, check-codecs, both lamdera make targets, npm test. Commit: 1d80415 (Fix folded month viewport seam). Orchestration log: `.squad/orchestration-log/2026-05-16T11:27:21Z-hicks.md`. Decision merged to `.squad/decisions/decisions.md`.

- 2026-05-16T18:30:00Z: Issue #53 now persists the frontend theme without migration work by treating `theme` as URL-backed page state in `src/Frontend.elm`. `init`/`UrlChanged` hydrate from the `?theme=` query, `ToggleTheme` rewrites the current route with `Nav.replaceUrl`, and internal navigation preserves the active theme unless the target URL explicitly overrides it. Regression coverage lives in `tests/FrontendTests.elm` for dark-theme bootstrap plus query rewrite behavior. Validation: `elm-format src/ tests/ --yes`, both `lamdera make` targets, `npm test` (75/75), and `lamdera live` on port 8000 with HTTP 200.

- 2026-06-06T14:17:10Z: Dialog mask wheel routing in `src/Frontend.elm` now respects PaackEng `Dialog.view`'s DOM shape by keeping backdrop wheel rerouting to scroll `#group-transactions-list` while a dialog is open, but adding a dialog-container wheel stop-propagation guard so the spending dialog keeps native wheel/touchpad scrolling. Explicit path kept tiny (`DialogMaskWheelScrolled` + pure `groupTransactionsDialogMaskScrollPlan`); progressive loading and month folds remain on existing helpers. Validation: `elm-format src/ tests/ --yes`, both `lamdera make` targets, `lamdera live --port=8011` with HTTP 200; `npm test` blocked by pre-existing unrelated theme expectation failure. Orchestration log: `.squad/orchestration-log/2026-06-06T14:17:48Z-hicks.md`. Decision merged to `.squad/decisions/decisions.md`.

- 2026-06-06T14:24:29Z: Explained the purpose of the `groupTransactionsDialogMaskScrollPlan` pure helper and repeated route guards in `src/Frontend.elm`. Finding: Pure scroll-plan helper is justified as a testable seam for scroll-position calculation, but the repeated inner route guard is unnecessarily defensive and can be simplified by removing the inner guard from the plan function while keeping the outer guard in the message handler. Orchestration log: `.squad/orchestration-log/2026-06-06T14:24:29Z-hicks.md`. Session log: `.squad/log/2026-06-06T14:24:29Z-scroll-plan-explanation.md`.
