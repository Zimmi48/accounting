# Orchestration: Bishop - Lifecycle Totals Bug Validation

**Agent:** Bishop  
**Spawned:** 2026-04-29T06:59:52Z  
**Mode:** background  

## Directive
Validate whether failing lifecycle-total tests expose real bugs or false positives in the backend delete/edit cycle.

## Outcome
**THE BUG IS REAL** (internal data corruption).

During delete/edit operations, `removeTransactionFromModel` correctly negates amounts but leaves zero-valued entries in `totalGroupCredits` dicts instead of removing them. This garbage accumulates in:
- Global `BackendModel.totalGroupCredits`
- Per-year, per-month, per-day aggregates
- `Person.belongsTo` set (group keys persist at zero)

**User impact:** Hidden by accident — `RequestUserGroups` handler filters by `credit < 0` and `credit > 0`, masking phantom groups. Not urgent but should be cleaned up.

**Manual reproduction:** Create spending → delete it → inspect backend state for zero-valued entries.

## Artifact
`.squad/decisions/inbox/bishop-test-validation-lifecycle-totals.md` — Full bug report with reproduction path and fix requirements.

## Next Step
Awaiting orchestration decision on cleanup strategy and test remediation.
